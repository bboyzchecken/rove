# secrets
.env
.env.local
*.pem

# go
apps/api/bin/
apps/api/tmp/
*.test
*.out

# node
node_modules/
.pnpm-store/
apps/web/.next/
apps/web/out/
apps/web/next-env.d.ts
*.tsbuildinfo

# tooling
.turbo/
coverage/
playwright-report/
test-results/

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# local data
docker/volumes/

# raw FLUX output — only the optimised copies in apps/web/public are committed
.assets-raw/

# terraform  (.terraform.lock.hcl is committed on purpose — it pins provider versions)
deploy/terraform/.terraform/
deploy/terraform/*.tfstate
deploy/terraform/*.tfstate.*
deploy/terraform/*.tfvars
!deploy/terraform/*.tfvars.example
deploy/terraform/backend.hcl
deploy/terraform/crash.log
deploy/terraform/tfplan
