# Dev Spec — Feedback #2 (UAT รอบ 1 → เตรียม UAT รอบ 2)

- **ที่มา:** `ROVE - Feedback #1 - Google สไลด์.pdf` (20 หน้า, "Update 12 Sep 2026") + เสียง `Soi Phahon Yothin 18.m4a` (16:35 นาที) — ถอดไว้ที่ [feedback-2-transcript.md](feedback-2-transcript.md)
- **ขอบเขต:** ทั้งเว็บ (`apps/web`) + API บางจุด (§0 D-16) · ทุกหน้าที่ผู้ทดสอบเปิด: ตัวละคร · สร้างทริป · ทริปของฉัน · สำรวจ · หน้าทริป · หน้าแรก
- **Branch:** `uat-doodle` → แตก `feedback-2/*` ต่อเฟส
- **สถานะ (14 ก.ย. 2569, เย็น):** **โค้ดครบทั้ง 6 เฟส** บน `feedback-2/phase-0` … `feedback-2/phase-6` (stacked branch ต่อกัน ยังไม่ push ไม่ merge) · ทุกข้อใน §0 คือคำตอบของพี่จากการถาม 14 ก.ย. 2569 ไม่ใช่การเดา · ของที่ยังรอผู้ทดสอบดู §8 · **ผ่าน** typecheck / eslint / vitest (130) / go test · e2e + `next build` ดู §7
- **ต้นตอร่วม (2 เรื่อง):**
  1. **flow "เริ่มจากอะไรก็ได้" ทำให้หน้าทริปของแต่ละคนเริ่มไม่เหมือนกัน** — แท็บ 11 อันเท่ากันหมด ไม่บอกว่าอันไหนต้องกรอก อันไหนแค่ตรวจ ผู้ทดสอบ (ซึ่งไม่คุ้นเว็บ) หลงว่าต้องไปไหนต่อ → หน้า 17, เสียง 11:51–13:32
  2. **กฎ "สี = ฟีเจอร์" (brand spec §2.5) ขัดกับสิ่งที่ผู้ทดสอบต้องการทุกหน้า** — แยกสีต่อทริป ต่อกล่อง ต่อสถานะ → ยกเลิกกฎ (D-2) แล้วทุกอย่างที่เหลือถึงทำได้

---

## 0. การตัดสินใจที่ล็อกแล้ว (อย่ารื้อ)

| # | เรื่อง | สรุป | ที่มา / เหตุผล |
|---|---|---|---|
| D-1 | รอบส่ง | **UAT รอบเดียว ครบทุกอย่าง** · ออกแบบ **มือถือ = คอม เท่ากัน** | ผู้ทดสอบเปิดทั้งสองแบบ เสียง 16:22 "รีเช็คในโทรศัพท์ด้วย ยังมั่วอยู่" |
| D-2 | กฎสี | **ยกเลิก "สี = ฟีเจอร์" (§2.5)** สีเป็นแค่ความสนุก ใช้ palette แบรนด์ได้อิสระ · แก้ `ROVE_BRAND_SPEC.md` และคอมเมนต์ที่อ้าง §2.5 | หน้า 8, 10, 18, 19 ขอ "แยกสี/เพิ่มสี" ทุกกล่อง — ทำไม่ได้ถ้ากฎยังอยู่ |
| D-3 | สีประจำทริป | **ระบบสุ่มจาก palette ตอนสร้าง หัวห้องเปลี่ยนได้** · ใช้สีเดียวกันที่ แท็บ upcoming / ปฏิทิน / หน้าทริป | เสียง 2:41 "สีของตัวแทนทริปนั้น" |
| D-4 | ตัวละคร | **แทนที่สัตว์ 20 ตัวทั้งหมด** ด้วยดอกไม้ **8 แฉก สมมาตร flat สีทึบ ไม่มีเส้นขอบ** · เป็น**ชุดสำเร็จรูป** (สี + หน้า) ไม่ให้ผสมเอง · ถ้าสีไม่ครบ 20 ให้หน้าตาต่างกันแทน · ชื่อ **TH/EN เหมือนเดิม** · user เก่า migrate อัตโนมัติ | หน้า 2 · ⚠ เสียง 0:32 ตีความว่า "มีแขนขาแบบตัวชมพู แต่หน้า/กลีบสมมาตรแบบม่วง-เหลือง" — **ยืนยันกับผู้ทดสอบก่อนเจนรูป** |
| D-5 | mock หลายแบบ | **ทำในแอปจริง** ไม่ใช่ prototype แยก · **ปุ่มลอยสลับ A/B/C** เปิดเฉพาะ env UAT ทุกคนเห็น | เรื่องที่ต้องให้ผู้ทดสอบเลือก: โครงแท็บหน้าทริป (2–3 แบบ) · ชุดสีปฏิทินวันว่าง (2 ชุด) · หน้าแรก (2 แบบ) |
| D-6 | ทริปของฉัน | **รวม `/trips` เข้า `/home`** เหลือเมนูเดียวชื่อ **"ทริปของฉัน / My Trips"** | หน้า 7 "ข้อมูลดูซ้ำซ้อนกัน" |
| D-7 | วันที่ | **date picker ของเราเอง** `dd/mm/yyyy` **ปี ค.ศ.** แทน `<input type="date">` ทุกที่ | หน้า 5 · native input แสดงต่างกันทุกเครื่อง |
| D-8 | ค่าเริ่มต้น flow สร้างทริป | **ว่างทั้งหมด** ทุกขั้น ทุกทางเริ่ม: วันที่ว่าง · สนามบินต้นทางไม่ prefill BKK · **จำนวนคนเริ่ม 1** | หน้า 5 "ทุกอันเอา กับคน เอาเริ่มต้น 1 คน ทั้งหมด" |
| D-9 | ทางเริ่มทริป | เปลี่ยนจากการ์ด 3 ใบ เป็นคำถาม **"มีอะไรแล้วบ้าง" เลือกได้หลายข้อ** แล้วเรียงขั้นถัดไปตามที่ติ๊ก | ต้นตอร่วม 1 · เคสจริง "รู้วัน + จองที่พัก + จองไฟลท์แล้ว" ไม่ตรงการ์ดไหนเลย |
| D-10 | ชนลิมิตแผนฟรี | **ทำ paywall ให้ชัด**: หน้าอธิบายลิมิต + ปุ่มซื้อ Trip Pass ฿299 + ปุ่มไปปิดทริปเก่า | หน้า 6 "กดต่อไม่ได้" · ตอนนี้เว็บไม่จับ 402 |
| D-11 | โครงหน้าทริป | **mock 2–3 โครง** ให้ผู้ทดสอบเลือก (D-5) · ทุกขั้น/แท็บมี **4 สถานะ: ต้องทำ / รอตรวจเช็ค / เรียบร้อย / ไม่จำเป็น (ข้ามได้)** · ลำดับแท็บจริงรอผู้ทดสอบส่ง (§4) | หน้า 17 · เสียง 11:51 "บางอันรวบกันได้" |
| D-12 | เช็คลิสต์เตรียมห้อง | ติ๊กอัตโนมัติ **+ กด "ข้าม" ได้** (= สถานะ "ไม่จำเป็น") · ข้อที่ยังไม่เสร็จ**กลายเป็นปุ่ม** · ความคืบหน้าแสดง **เปอร์เซ็นต์ + ข้อความ** ("พร้อม 40% · เหลืออีก 3 อย่าง") · เอากล่อง "ยังไม่มีวันเดินทาง" ที่ซ้ำออก · เก็บ "สร้างห้องทริป" ไว้ ข้ออื่นให้ mock โครงแท็บกำหนดเอง | หน้า 17 · เสียง 12:01–13:32 |
| D-13 | ปฏิทินวันว่าง | **แท็บเดียว** (ยุบ "ใส่วันว่างของฉัน / เลือกช่วงทริป") · **กดแล้วขึ้นสีทันที ไม่มีกรอบ** · สีตามจำนวนคนที่ว่างตรงกัน 4 ระดับ · **วันที่เรากดเองมีเครื่องหมายทับสี** · มีปุ่ม **"ยืนยันวันว่าง"** → ระบบ**เด้งช่วงที่ดีที่สุด** → **หัวห้องล็อค** · สี **2 ชุดสลับดูใน UAT**: ชุดผู้ทดสอบ (ส้ม/ฟ้า/ชมพู/เขียว หยิบจาก token แบรนด์) + ชุดสีเดียวไล่เข้ม | หน้า 20 · เสียง 14:42–16:05 "พี่ลองไปรีเช็คสี มันมีสีของ design system อยู่" |
| D-14 | รูปปกทริป | **คงกลไกปกแบบ Facebook** (แถบตายตัว ครอปเต็ม) · แก้ **ระบบอัปโหลดที่พัง** + ฟอร์มบอกขนาด/ratio ที่แนะนำ + ลากจัดตำแหน่ง | เสียง 10:06–11:40 · ที่ผู้ทดสอบงงเพราะอัปเองไม่ได้ ไม่ใช่เพราะกลไก |
| D-15 | ทริปที่ผ่านมา | **การ์ดเดิม** ชื่อใหญ่ขึ้น รูปเล็กลง · **ไม่ทำ** list กดขยายข้าง | เสียง 5:22 พี่บอกเองว่า "ใช้ยากกว่า UX ไม่ดี" และผู้ทดสอบยอม 7:17 · **ทับคำตอบ PDF หน้า 12** |
| D-16 | แตะ API | **แตะได้** เฉพาะที่จำเป็น: `trips.color` · `trip_step_overrides` (ข้าม) · characters seed ใหม่ · trending (view รายวัน) · availability confirm/lock · paywall 402 payload | ต่างจาก Feedback #1 ที่ห้ามแตะ API — รอบนี้ของที่ขอเป็น data ไม่ใช่แค่หน้า |
| D-17 | หน้าแรก | **2 แบบสลับดู**: **A** = โครงเดิม + headline/tagline ใหม่ (เจน 3–5 ชุดให้เลือกก่อน) · **B** = หน้าใหม่ทั้งหมด ใช้ skill `ui-ux-pro-max` + รูปจาก BFL ตาม brand spec §7 **ต้องไม่ซ้ำแบบที่เคยทำ** · แก้ layout มือถือทั้งสองแบบ | เสียง 16:08–16:30 |
| D-18 | สำรวจ | ชิป **ทั้งหมด / ยอดนิยม / มาใหม่ / ติดเทรนด์** (ติดเทรนด์ = ยอดดูพุ่งใน 7 วัน) · **เอาชิปเมืองออก** · เพิ่ม **filter เลือกได้หลายประเทศ** แบบเว็บ travel ใหญ่ · การ์ดแบบ ref: **ข้อมูลใหญ่บน รูปล่าง** | หน้า 15 · เสียง 9:09 "อนาคต 10–100 ประเทศ" |
| D-19 | Dream Trip | **stack ซ้อนกันตาม ref** มีธงประเทศ · ปุ่ม "+ Add More Dream Trip" ล่างสุด · เพิ่มปุ่ม **"เก็บเป็น Dream"** ในหน้าทริป | หน้า 12 · เสียง 7:23–8:50 (พี่ค้าน stack แต่ผู้ทดสอบยืน) |
| D-20 | สมาชิกในหน้าทริป | **วงกลม + จุดสถานะ** สั้นๆ · จุด = **ใส่วันว่างครบ + ใส่ที่อยากไปแล้ว** (เขียว/เหลือง/เทา) | หน้า 19 · เสียง 14:23 "สั้นๆ ไม่ต้องยาวยืด" · เดิมดูแค่ `hasWishlist` |

---

## 1. เฟส 0 — ฐานที่ทุกเฟสต้องใช้ (ทำก่อน)

> ของกลางที่ถ้าไม่มี เฟส 1–5 จะทำซ้ำกันเอง

### F0.1 ปุ่มลอยสลับ variant (D-5) ✅
**แก้:** `components/uat/variant-switcher.tsx` — ปุ่มลอยมุมล่างขวา แสดงเมื่อ `NEXT_PUBLIC_UAT_VARIANTS=1` · เก็บค่าใน cookie `rove-variant.<key>` (ไม่ใช่ localStorage เพื่อให้ SSR ตรงกับ client) · hook `useVariant('trip-tabs' | 'avail-colors' | 'landing')` · แต่ละ key มีรายการ variant + label ภาษาไทย

### F0.2 Date picker ของเราเอง (D-7) ✅
**ต้นตอ:** ทุกที่ใช้ `<Input type="date">` — [new-trip-flow.tsx:387-412](../apps/web/components/trip/new-trip-flow.tsx#L387), [route-builder.tsx:213-240](../apps/web/components/trip/route-builder.tsx#L213) — เครื่องผู้ทดสอบขึ้น `mm/dd/yyyy` และ `04/12/2026` อ่านเป็น 4 ธ.ค. หรือ 12 เม.ย. ก็ได้

**แก้:** `components/ui/date-field.tsx` — ช่องแสดง `dd/mm/yyyy` (ค.ศ.) + popover ปฏิทินเดือน ใช้ `availability-calendar.tsx` เป็นฐานเดียวกัน (D-13 ต้องรื้อตัวนั้นอยู่แล้ว) · รองรับ `min`/`max` · ว่างได้จริง (ไม่มี default)

**ผลพลอยได้:** ปิด bug "46365 วัน" — [domain.ts:34-37](../apps/web/lib/data/domain.ts#L34) `parseIsoDate('')` ได้ปี 1900 · เพิ่ม guard คืน `null` เมื่อว่าง และ [new-trip-flow.tsx:411](../apps/web/components/trip/new-trip-flow.tsx#L411) ซ่อนตัวนับวันจนกว่าจะมีทั้งสองวัน

### F0.3 สีประจำทริป (D-3) ✅
**API:** migration เพิ่ม `trips.color varchar(16)` · สุ่มจาก 6 สี palette ตอน create (หลีกเลี่ยงสีซ้ำกับทริป active ล่าสุดของคนสร้าง) · PATCH ให้เฉพาะ owner
**Web:** `Trip.color` ใน domain + mapper · `lib/trip-color.ts` คืน class `bg-*-light` / `bg-*-solid` · ช่องเลือกสีในหน้าตั้งค่าทริป (owner)

### F0.4 ตัวละครดอกไม้ (D-4) ✅
1. **ก่อนเจน:** ส่งภาพร่าง 2–3 ทางให้ผู้ทดสอบยืนยันข้อ ⚠ ใน D-4 (แขนขา? หน้าแบบไหน?) → **ทำเป็น 3 look ในแอปจริง** สลับได้ที่ปุ่มลอย (key `flower`: 1 มีแขนขา · 2 หน้าอย่างเดียว · 3 มีตัว+แขนขา) ค่าเริ่มต้น = 1 ตามที่พี่ตีความ
2. ~~เจน 20 ชุดด้วย BFL~~ → **วาดเป็น SVG จาก spec เดียว** ([lib/catalog/flowers.ts](../apps/web/lib/catalog/flowers.ts)) เพราะรูปทรง 8 แฉก flat ทึบ ไม่มีเส้นขอบ วาดด้วยเรขาคณิตได้ตรงกว่าเจน (brand spec §7.5 พูดเรื่องเดียวกันกับ doodle) · ได้ 20 ชุด (พื้น + กลีบ + กลาง + หน้า 7 แบบ) ไม่ซ้ำกัน · ไฟล์นิ่ง `public/characters/flower-NN.svg` + `characters.json` เขียนจาก `scripts/gen-flower-characters.mjs` · ลบ `char-*.webp` แล้ว · ถ้าผู้ทดสอบอยากได้แบบเจนจริงค่อยเจนทับ ระบบไม่ต้องแก้
3. [characters.ts:10](../apps/web/lib/catalog/characters.ts#L10) + [apps/api/data/characters.json](../apps/api/data/characters.json) ชุดใหม่ 20 id · ชื่อ TH/EN
4. migration: map `users.character_id` เก่า → ใหม่ตามลำดับ index (shiba→flower-01 …) · fallback `'flower-01'` แทน `'shiba'`
5. เช็คทุกที่ที่ใช้ [character-avatar.tsx](../apps/web/components/ui/character-avatar.tsx) — ขนาด xs ยังอ่านหน้าออกไหม

### F0.5 แก้ brand spec (D-2) ✅
`ROVE_BRAND_SPEC.md` §2.5 → เขียนใหม่เป็น "สีใช้แยกของ (ทริป/กล่อง/สถานะ) ได้ แต่ห้ามเกิน 6 สีต่อจอ และข้อความยังต้องเป็น ink บนพื้นอ่อน (§2.4)" · ลบการอ้าง §2.5 ใน [tilted-tag.tsx](../apps/web/components/brand/tilted-tag.tsx) และ `docs/feedback-1-fix-list.md` D-4/D-7 ใส่หมายเหตุว่าถูกทับด้วย D-2 รอบนี้

### F0.6 สถานะ 4 แบบต่อขั้น (D-11) ✅
`lib/trip-progress.ts` — ฟังก์ชันเดียว `stepStatus(trip, step) → 'todo' | 'check' | 'done' | 'skipped'` ใช้ร่วมกันโดย: เช็คลิสต์ (F3.4) · แท็บ (F3.3) · ลิสต์ Planning (F2.3) · จุดสมาชิก (F3.7)
**API:** `trip_step_overrides(trip_id, step, status='skipped', by_user_id)` + endpoint PATCH · เก็บ `trips.started_with` (json ของที่ติ๊กใน F1.1) เพื่อเรียงขั้น

---

## 2. เฟส 1 — สร้างทริป (หน้า 4–6)

### F1.1 "มีอะไรแล้วบ้าง" แทนการ์ด 3 ทาง (D-9) — หน้า 4–6 ✅
**ต้นตอ:** [new-trip-flow.tsx:57-66](../apps/web/components/trip/new-trip-flow.tsx#L57) `ENTRIES` 3 ทางเลือกทางเดียว

**แก้:** step 0 เป็น checkbox หลายข้อ: ☐ รู้วันแล้ว ☐ จองไฟลท์แล้ว ☐ จองที่พักแล้ว ☐ รู้ปลายทางแล้ว ☐ มีเพื่อนไปด้วยแล้ว · ไม่ติ๊กเลยก็ไปต่อได้ (= เดิม "ยังไม่รู้วัน") · ลำดับ step ถัดไปเรียงตามที่ติ๊ก: ติ๊กไฟลท์ → ขึ้นฟอร์มเที่ยวบินก่อน · ติ๊กวัน → ขึ้นวันก่อน · ที่พัก → ช่องวางลิงก์/ชื่อที่พัก (ใหม่ เล็กๆ) · เก็บผลลง `trips.started_with` (F0.6)

### F1.2 ค่าเริ่มต้นว่างทั้งหมด (D-8) — หน้า 5 ✅
- ลบ `DEFAULT_START/END` ([:73-74](../apps/web/components/trip/new-trip-flow.tsx#L73)) · `HOME_AIRPORT` ([:76](../apps/web/components/trip/new-trip-flow.tsx#L76)) ไม่ prefill (เก็บค่าไว้ใช้เป็น *suggestion แถวแรก* ใน airport picker ได้)
- `useState(4)` → `useState(1)` ([:101](../apps/web/components/trip/new-trip-flow.tsx#L101)) · `SAMPLE_TICKET` ยังวางได้แต่ไม่ใช่ค่าตั้งต้น
- ทุก step ใช้ `DateField` (F0.2)

### F1.3 ช่องสนามบินสูงเท่ากัน — หน้า 4 ✅
**ต้นตอ:** [airport-picker.tsx:112](../apps/web/components/trip/airport-picker.tsx#L112) ค่าที่เลือกแล้ว = ปุ่มเทาทึบ · [:132-146](../apps/web/components/trip/airport-picker.tsx#L132) ว่าง = ช่องค้นหา + ข้อความ "ค้นหาสนามบินได้ทั่วโลก" ห้อยใต้ → สูงต่างกัน

**แก้:** สถานะเดียว: กล่องเดียวกัน ความสูงเดียวกัน · มีค่า = โชว์ code + ชื่อ + × ในกล่อง · ว่าง = placeholder ในกล่อง · เอาข้อความห้อยออก · ทำร่วมกับ F1.2 (ทั้งสองช่องเริ่มว่าง)

### F1.4 Paywall (D-10) — หน้า 6 ✅
**ต้นตอ:** [trip.handler.go `checkTripAllowance`](../apps/api/pkg/handlers/api/trip.handler.go) ตอบ 402 · [api-client.ts](../apps/web/lib/api-client.ts) ไม่จับ 402 · ข้อความโผล่ในกล่องเทา [new-trip-flow.tsx:553-557](../apps/web/components/trip/new-trip-flow.tsx#L553)

**แก้:** API 402 ส่ง `{code:'TRIP_LIMIT', activeTrips:[{id,title}], price:299}` · web: `components/billing/trip-limit-sheet.tsx` — อธิบาย 1 บรรทัด + รายการทริป active พร้อมปุ่ม "ปิดทริปนี้" + ปุ่ม "ซื้อ Trip Pass ฿299" → `/pricing` · แจ้งล่วงหน้าที่ step 0 เป็นแถบเล็กถ้า quota เต็มอยู่แล้ว (ไม่ให้กรอกจนจบแล้วเพิ่งชน)

---

## 3. เฟส 2 — ทริปของฉัน (หน้า 7–13, เสียง 0:42–8:50)

### F2.1 รวมหน้า + เมนู (D-6) — หน้า 7 ✅
- [app-shell.tsx:39](../apps/web/components/common/app-shell.tsx#L39) เมนู `สรุปของฉัน` + `ทริปของฉัน` → เหลือ **ทริปของฉัน / My Trips** ชี้ `/home`
- `/trips` → redirect `/home` (ลิงก์เก่าใน invite/email ยังใช้ได้) · [trip-list.tsx](../apps/web/components/trip/trip-list.tsx) ยุบเข้า home
- ลำดับ section ใหม่ในหน้า: หัว → ปุ่ม 3 → **ทริปที่กำลังจะมาถึง** → **กำลังวางแผน** (F2.3) → ปฏิทิน (F2.4) → สรุปปี (F2.5) → ทริปที่ผ่านมา (F2.6) → Dream (F2.7)

### F2.2 หัวหน้า + ปุ่ม — หน้า 8 ✅
- ตัด "อีก 88 วัน" ใต้ชื่อ ([home-screen.tsx ~:101](../apps/web/components/home/home-screen.tsx)) — ตัวเลขนี้ไปอยู่บนแท็บ upcoming แทน
- ปุ่มเหลือง `ชวนเพื่อนเข้าทริป` → **`+ สร้างทริปใหม่`** ชี้ `/new` · อีก 2 ปุ่มคงเดิม

### F2.3 upcoming เป็นแท็บ + ลิสต์ Planning — หน้า 8–9, เสียง 0:42–2:08 ✅
**แก้:** ลบการ์ดปกใหญ่ · แต่ละทริป = แถบสีประจำทริป (F0.3) ความสูงเท่ากัน เรียงลงล่างตามวันใกล้สุด: ซ้าย **เลขวันนับถอยหลังใหญ่** · กลาง ธงประเทศ (emoji) + ชื่อทริป + ช่วงวัน + info 1 บรรทัด · ขวา `CharacterStack` เพื่อน
ต่อท้ายด้วยหัวข้อ **"กำลังวางแผน"** = ทริปที่ไม่มีวัน · แท็บเดียวกันแต่ซ้ายเป็นป้าย `Planning` แทนเลข · **กดแล้วกางออกในที่**: ปลายทาง · เหลืออะไร (จาก `stepStatus` F0.6: "ยังไม่กำหนดวัน · ยังไม่สรุปสถานที่ · ยังไม่คอนเฟิร์มคน") · "อีก N ขั้น" · ปุ่มเข้าห้องทริป
**ข้อมูล:** `useUpcomingTrips` ต้องคืนทั้งมีวัน/ไม่มีวัน · mock [user.ts:98](../apps/web/lib/data/mock/seed/user.ts#L98) เลิก hardcode 88

### F2.4 ปฏิทินทริป ปี + เดือน — หน้า 10–11, เสียง 2:08–3:36 ✅
**แก้:** ลบแถบ progress ([~:186](../apps/web/components/home/home-screen.tsx)) · `components/home/trip-calendar.tsx` เปิดมาเป็น **ปี**: ตาราง 12 เดือน (3×4 บนคอม, 2×6 มือถือ) แต่ละเดือนเป็นกริดวันเล็ก มี**แท่งสีประจำทริป**คร่อมช่วงวัน ป้ายเป็น**ประเทศ/เมือง ไม่ใช่ชื่อทริป** (เสียง 3:19) · กดเดือน → **เดือน**: ปฏิทินเต็ม เส้นยาวคร่อมวัน เขียนชื่อทริป · กดแท่ง → `/t/[id]` · ปุ่มสลับ ปี/เดือน

### F2.5 สรุปปี 4 สี + ตัดกราฟ — หน้า 10, เสียง 3:27–4:43 ✅
- การ์ด 4 ใบ ([~:236-255](../apps/web/components/home/home-screen.tsx)) ใส่พื้น 4 สีต่างกัน (D-2)
- **ลบกราฟแท่งรายเดือน** ([~:258](../apps/web/components/home/home-screen.tsx)) และ `stats.monthlyDays` ถ้าไม่มีใครใช้ต่อ — ผู้ทดสอบสรุปเอง "ตัดไปเลย ไม่สำคัญ" (4:39)

### F2.6 ทริปที่ผ่านมา (D-15) — เสียง 4:43–7:20 ✅
การ์ดเดิม ([~:277](../apps/web/components/home/home-screen.tsx)) · ชื่อทริปขึ้น 1 ขั้นใน type scale · รูปปกลดสัดส่วนจาก ~60% เป็น ~40% ของการ์ด · ตัวเลข วัน/ที่/บาท ให้ใหญ่กว่า label · **ไม่ทำ** list ขยายข้าง

### F2.7 Dream Trip stack (D-19) — หน้า 12–13, เสียง 7:23–8:50 ✅
`components/profile/dream-stack.tsx` แทนลิสต์ชมพู ([~:312](../apps/web/components/home/home-screen.tsx)) · การ์ดสีต่างกันซ้อนทับแนวตั้ง (เห็นหัวการ์ดถัดไป ~48px) · ธงประเทศใหญ่มุมขวา · กดการ์ด = ยกขึ้นบนสุด/เข้า `/dreams` · ใบล่างสุดเป็นกล่องเล็ก **"+ Add More Dream Trip"** · ในหน้าทริป (F3.x) เพิ่มปุ่ม "เก็บเป็น Dream" ที่ header menu → สร้าง dream จากปลายทางของทริป

---

## 4. เฟส 3 — หน้าทริป (หน้า 17–20, เสียง 10:06–16:05)

### F3.1 header: ชื่อ + สเตตัส + ปก (D-14) — หน้า 17 ✅
- [trip-header.tsx:102](../apps/web/components/trip/trip-header.tsx#L102) ชื่อทริปขึ้นเป็น display size · สเตตัส pill ([:85](../apps/web/components/trip/trip-header.tsx#L85)) ย้ายไป**หลังชื่อ**บนคอม / **บรรทัดใต้ชื่อ**บนมือถือ (เสียง 11:41)
- ปก: คงแถบ ([:70](../apps/web/components/trip/trip-header.tsx#L70)) · แก้ upload ใน [trip-cover-sheet.tsx:93-140](../apps/web/components/trip/trip-cover-sheet.tsx#L93) — **ไล่ให้เจอว่าพังตรงไหน (ผู้ทดสอบอัปไม่ได้)** ก่อนแตะ UI · เพิ่มข้อความ "แนะนำ 1200×800 (3:2) · ไฟล์ไม่เกิน 5MB" · ลากเลื่อนจัดตำแหน่งครอปหลังอัป · placeholder รูปวาดขยายเต็มแถบไม่ลอยกลางขาว

### F3.2 ปุ่ม "เก็บเป็น Dream" (D-19) — ใน header menu ตาม F2.7 ✅
### F3.3 โครงแท็บ 2–3 แบบ (D-11) — หน้า 17, เสียง 11:51 ✅
**ต้นตอ:** [trip-tabs.tsx:18-28](../apps/web/components/trip/trip-tabs.tsx#L18) 11 แท็บเท่ากันหมด ไม่มีสถานะ

**ทำ 3 variant ใต้ `useVariant('trip-tabs')`** — ทุกแบบใช้ `stepStatus` (F0.6) และ**ลำดับตาม `started_with`** (ขั้นที่ทำแล้วไปอยู่ท้าย/ยุบ):
- **A — แท็บเดิม + ป้ายสถานะ:** 11 แท็บคงชื่อ แต่ทุกแท็บมีจุด/ป้าย 4 สถานะ · เรียงใหม่ตาม started_with · ปุ่ม "ขั้นต่อไป →" ใต้ header พาไปแท็บ todo แรก
- **B — จัดกลุ่ม 4 ช่วง:** `วางแผน` (วันเดินทาง · ที่อยากไป · แพลน) / `จอง+เตรียม` (การจอง · งบ · เตรียมตัว · เอกสาร) / `ระหว่างทริป` (ค่าใช้จ่าย · รูป · คุยกัน) / `ภาพรวม` — ช่วงที่ยังไม่ถึงยุบเป็นหัวเดียว
- **C — หน้าเดียวทำทีละขั้น:** ไม่มีแท็บบน ภาพรวมเป็นรายการขั้นตอนแนวตั้ง (สถานะ + ปุ่ม) กดเข้าไปทำแล้วกลับมา
- ลำดับแท็บจริงจากผู้ทดสอบ (ยังไม่ส่ง) จะมาทับ A/B ตอนได้รับ (§6)

### F3.4 เช็คลิสต์เตรียมห้อง (D-12) — หน้า 17, เสียง 12:01–13:32 ✅
[trip-overview.tsx:88-137](../apps/web/components/trip/trip-overview.tsx#L88): หัวเป็น **"พร้อม 40% · เหลืออีก 3 อย่าง"** · ข้อ done = ติ๊กเขียว ไม่กดได้ · ข้อ todo = **ปุ่ม** (สีทึบ + ลูกศร) · ข้อ check = ปุ่มโปร่ง "ตรวจดู" · ทุกข้อ todo/check มี "ข้าม" เล็กๆ → skipped (F0.6) · **ลบกล่อง "ยังไม่มีวันเดินทาง"** ([:141-152](../apps/web/components/trip/trip-overview.tsx#L141)) เพราะข้อ "ล็อควันเดินทาง" เป็นปุ่มแล้ว · รายการข้อขึ้นกับ variant F3.3 (C ใช้เช็คลิสต์เป็นหน้าหลักเลย)

### F3.5 การ์ดสรุป + สถานะตอนนี้ แยกสี — หน้า 18–19 ✅
`Stat` ([:166-200](../apps/web/components/trip/trip-overview.tsx#L166)) 4 ใบ 4 สี · "สถานะตอนนี้" ([:253-310](../apps/web/components/trip/trip-overview.tsx#L253)) แต่ละกล่องแยกช่อง แยกสี (ผู้ทดสอบ: "สวยแล้ว แต่ขอเป็นแยกช่องและสีทุกอัน")

### F3.6 สมาชิก (D-20) — หน้า 19, เสียง 14:12–14:30 ✅
[trip-overview.tsx:209-250](../apps/web/components/trip/trip-overview.tsx#L209) → แถววงกลม `CharacterAvatar` size lg + ชื่อใต้ + **จุดสถานะ**มุมล่างขวา: เขียว = วันว่างครบ + ที่อยากไปแล้ว · เหลือง = ครบอย่างเดียว · เทา = ยังไม่ทำ · แตะวงกลม → tooltip บอกว่าขาดอะไร · ปุ่ม "ชวนเพิ่ม" เป็นวงกลม + ท้ายแถว

### F3.7 ปฏิทินวันว่าง (D-13) — หน้า 20, เสียง 14:42–16:05 ✅
**ต้นตอ:** [date-board.tsx:56,179-193](../apps/web/components/dates/date-board.tsx#L56) 2 แท็บ · [availability-calendar.tsx:47-56](../apps/web/components/dates/availability-calendar.tsx#L47) `heatClass` ใช้กรอบ + สีอ่อน

**แก้:**
1. ยุบ 2 แท็บเหลือปฏิทินเดียว · แตะวัน = toggle ว่างของฉัน (optimistic) → สีวันคำนวณจากจำนวนคนตรงกันทันที
2. สี 4 ระดับ ไม่มีกรอบ: ไม่มีใครตรง / ส่วนน้อย (<50%) / เกือบครบ / ครบ · **วันที่ฉันเลือก** มีวงขอบหนา/จุดตัวละครทับ — เห็นทั้งสีกลุ่มและของตัวเอง
3. `useVariant('avail-colors')`: **ชุด 1** ส้ม/ฟ้า/ชมพู/เขียว จาก `--feature-*` token · **ชุด 2** เขียวไล่ 4 ขั้น
4. ปุ่มล่าง **"ยืนยันวันว่างของฉัน"** → บันทึก + toast · เมื่อสมาชิกยืนยันครบ (หรือหัวห้องกด "ดูช่วงที่ดีที่สุด") ระบบเด้งการ์ด **ช่วงที่คนว่างมากสุด 1–3 ช่วง** (`pickWindow` ที่มีอยู่) · **หัวห้องเท่านั้น**เห็นปุ่ม "ล็อคช่วงนี้" (`lock.mutate` [~:288](../apps/web/components/dates/date-board.tsx#L288)) · สมาชิกเห็น "รอหัวห้องล็อค"
5. legend ([:191-197](../apps/web/components/dates/availability-calendar.tsx#L191)) ตามชุดสีที่เลือก
**API:** เพิ่ม `availability.confirmed_at` ต่อสมาชิก เพื่อรู้ว่าใครยืนยันแล้ว

---

## 5. เฟส 4 — สำรวจ (หน้า 15, เสียง 8:50–10:01)

### F4.1 ชิป + ติดเทรนด์ (D-18) ✅
- [explore-screen.tsx:41-46](../apps/web/components/public/explore-screen.tsx#L41) `QUICK_FILTERS` → `ทั้งหมด / ยอดนิยม / มาใหม่ / ติดเทรนด์` · ยุบ toggle sort ([:151-165](../apps/web/components/public/explore-screen.tsx#L151)) เข้าชิปเดียวกัน
- **API:** `new` เปลี่ยนจาก `updated_at` เป็น `published_at DESC` ([trip.store.go:180-185](../apps/api/pkg/store/trip/trip.store.go#L180)) · `trending` = ตาราง `trip_view_daily(trip_id, day, views)` bump จาก `BumpViewCount` · คะแนน = views 7 วันล่าสุด ÷ (views 7 วันก่อนหน้า + 1) · ถ้าข้อมูลยังน้อย fallback เป็น popular

### F4.2 filter หลายประเทศ (D-18) ✅
ปุ่ม "ประเทศ" ข้างช่องค้นหา → sheet รายการประเทศที่มีทริป public (ธง + ชื่อ + จำนวน) เรียงตามจำนวน · ติ๊กได้หลายอัน · ค้นหาในลิสต์ · ประเทศที่เลือกโชว์เป็นชิปถอดได้ใต้ช่องค้นหา (แบบ Booking/Airbnb) · **API:** `GET /public/trips?countries=JP,KR` + endpoint นับต่อประเทศ

### F4.3 การ์ดแบบ ref (D-18) ✅
[explore-screen.tsx:264-285](../apps/web/components/public/explore-screen.tsx#L264): ข้อมูลอยู่**บน** — ชื่อทริปตัวใหญ่ · แถวป้าย วัน / เมือง / งบ ต่อคน เป็นชิปสี · รูปวาดอยู่**ล่าง**เต็มกว้าง · ยอดดู/โคลน/รีวิว เป็นแถวไอคอนเล็กล่างสุด

---

## 6. เฟส 5 — หน้าแรก (D-17, เสียง 16:08–16:30)

### F5.1 headline/tagline 3–5 ชุด → ส่งผู้ทดสอบเลือกก่อน ✅
ยึด D-2 ของ Feedback #1 (ใครก็ได้ กี่คนก็ได้ ที่ไหนก็ได้) · ไม่ใช้คำว่า "กลุ่ม/หมู่คณะ" · เขียนเป็นไฟล์ `docs/feedback-2-landing-copy.md` ให้เลือกก่อนโค้ด

### F5.2 variant A — โครงเดิม + copy ที่เลือก + แก้มือถือ ✅
[app/(marketing)/page.tsx](../apps/web/app/(marketing)/page.tsx) · ไล่ทุก section ที่ 375px: hero tags, mosaic, ขั้นตอน 1–4, บล็อกตัวละคร (ดอกไม้ใหม่ F0.4)

### F5.3 variant B — หน้าใหม่ทั้งหมด ✅
`app/(marketing)/landing-b/page.tsx` ใต้ `useVariant('landing')` · ขั้นตอน: รัน skill `ui-ux-pro-max` เลือก style/palette/font pairing ที่**ไม่ใช่** doodle + pastel เดิม → เขียน layout ใหม่ → เจนรูป hero/section ด้วย BFL (§7 ปรับ base prompt ตาม style ใหม่) → ทดสอบ 375 / 768 / 1280 · ต้องใช้ตัวละครดอกไม้และตัวเลขจริงตาม [social-proof.ts](../apps/web/lib/social-proof.ts) เหมือนกัน

---

## 7. เฟส 6 — ปิดรอบ ✅

- QA มือถือ (375) + คอม ทุกหน้าใน §1–5 ผ่านการดูจริงในเบราว์เซอร์ (D-1) · dark mode ไม่มี
- i18n: เพิ่ม namespace `nav` / `status` / `explore` / `dates` / `home` / `variants` ใน `messages/th.json` + `en.json` และต่อกับ nav, ป้ายสถานะ 4 แบบ, ชิปสำรวจ, ปุ่มปฏิทินวันว่าง · ข้อความอื่นยังเป็นไทยในคอมโพเนนต์เหมือนเดิม
- ✅ ต่อข้อทำแล้วในไฟล์นี้ · **ผู้ทดสอบยังไม่ได้เลือก variant** — ค่าเริ่มต้นที่ deploy คือ: แท็บ **A** · สีปฏิทิน **ชุด 1** · หน้าแรก **A** · ดอกไม้ **แบบ 1** (แก้ที่ `lib/uat/variants.ts` → `default`)
- **วิธีเปิด UAT:** build ด้วย `NEXT_PUBLIC_UAT_VARIANTS=1` (Workers Builds → Variables) ปุ่มลอยมุมล่างขวาถึงจะขึ้น · production ไม่ตั้ง → ไม่มีปุ่ม cookie ไม่มีผล
- **API ต้อง migrate ก่อน deploy web** (§9): migration ใหม่ 5 ตัว `202609140000`–`202609140004` (สีทริป · ขั้น/started_with · ดอกไม้ · ประเทศใน dream · view รายวัน + published_at) แล้ว `go run . seed` เพื่อ upsert ดอกไม้ 20 ตัว
- **e2e (Playwright, desktop project) ผ่าน 17/17** หลังแก้ spec ให้ตรง flow ใหม่ (หน้าแรกของ `/new`, ปฏิทินวันว่างแบบเดียว, ตัวเลขแต้ม 1,970 กับ 11 รายการ prep ที่ spec เก่าค้างไว้) · ต้องรันชี้ `E2E_BASE_URL=http://localhost:3100` ถ้าใช้ dev server ที่เปิดอยู่ (127.0.0.1 โดน 403 จาก dev server ของ Next) · โปรเจกต์ `phone` ยังไม่ได้รัน (ต้องติดตั้ง WebKit)
- e2e เจอบั๊กจริง 2 จุดแล้วแก้ไป: (1) seed มี 2 ห้องเปิดอยู่แต่แผนฟรีให้ 1 → ผู้ทดสอบใหม่ชนกำแพงตั้งแต่ทริปแรก (หน้า 6 รอบที่แล้วก็เพราะอันนี้) → seed ให้ทั้ง 2 ห้องมี Trip Pass · (2) แตะช่องสนามบินก่อน React hydrate เสร็จ → รายการไม่เด้ง → แก้ใน `airport-picker.tsx` + prefetch รายการสนามบินยอดนิยมตั้งแต่ mount
- **ยังไม่ push / ไม่ merge** — รอพี่ดูก่อน · branch: `feedback-2/phase-0` → `phase-1` → … → `phase-6` (แต่ละอัน commit เดียว ต่อจากอันก่อน) · จะ merge เข้า `uat-doodle` ทีเดียวจาก `phase-6` ก็ได้

---

## 8. ยังไม่ทำ / รอข้อมูล

| เรื่อง | รออะไร | ทำอะไรระหว่างรอ |
|---|---|---|
| ลำดับแท็บจริง | ผู้ทดสอบบอกจะส่ง (เสียง 11:51) | ทำ A/B/C ไปก่อน ลำดับที่ส่งมาทับ A |
| หน้าตาดอกไม้ (แขนขา?) | ยืนยัน ⚠ ใน D-4 ด้วยภาพร่าง | ทำ F0.4 ข้อ 1 ก่อน ไม่เจน 20 ตัวจนกว่าจะยืนยัน |
| ชุดสีปฏิทินชุด 2 | พี่รีวิวชุดที่เสนอใน F3.7 | ทำแล้วทั้ง 2 ชุด สลับที่ปุ่มลอย (`avail-colors`) |
| headline หน้าแรก | ผู้ทดสอบเลือกจาก F5.1 | 5 ชุดอยู่ใน [feedback-2-landing-copy.md](feedback-2-landing-copy.md) · แบบ A ใช้ชุด 1, แบบ B ใช้ชุด 4 ไปก่อน |
| ชื่อ 4 สถานะที่ user เข้าใจ | ทดสอบคำใน UAT | ใช้ "ต้องทำ / ตรวจดู / เรียบร้อย / ข้ามแล้ว" ไปก่อน (`messages/*.json` → `status`) |
| ชื่อดอกไม้ | D-4 บอก "ชื่อ TH/EN เหมือนเดิม" — ตอนนี้ดอกไม้ 01–20 ใช้ชื่อสัตว์เดิม (ชิบะ, แมวส้ม …) ตามตัวอักษร | ถ้าพี่หมายถึง *รูปแบบ*ชื่อ ไม่ใช่คำเดิม แก้ที่ `FLOWER_SET` ในไฟล์เดียว |
| ธงประเทศเป็น emoji | Windows ไม่มี glyph ธง จะเห็นเป็นตัวอักษร 2 ตัว (เหตุผลเดียวกับ Feedback #1 D-4) | ตอนนี้ใช้ emoji ตาม F2.3/D-19 · ถ้าผู้ทดสอบเช็คบน Windows แล้วไม่โอเค เปลี่ยนเป็น SVG ธงได้ที่ `lib/data/countries.ts` จุดเดียว |
| อัปโหลดรูปปกพัง (D-14) | ยังไม่รู้ error จริงจากเครื่องผู้ทดสอบ | แก้สาเหตุที่น่าจะเป็นทั้งหมดแล้ว: รับ HEIC/ทุก image, หมุนตาม EXIF, ข้อความบอกชัดเมื่อเปิดไม่ได้, บอกขนาดก่อนกด, ลากจัดตำแหน่ง · ถ้ายังพัง ขอ screenshot ข้อความ error |
| ยอดเงินเป็นกราฟเส้น | ผู้ทดสอบเสนอแล้วถอนเอง (4:39) | **ไม่ทำ** |
| list ทริปที่ผ่านมาแบบขยายข้าง | ตัดโดย D-15 | **ไม่ทำ** |

---

## 9. จุดที่ต้องระวัง

- **Next 16.3.3** — `apps/web/AGENTS.md` เตือน breaking changes · route ใหม่ (`landing-b`, redirect `/trips`) เช็ค `next.config` ก่อน
- **Data mode** — `NEXT_PUBLIC_DATA_MODE=mock` เป็น default · ทุกฟิลด์ใหม่ (`color`, `started_with`, overrides, `confirmed_at`, trending) ต้องมีใน mock repo **และ** live mapper ไม่งั้น UAT (live) กับ dev (mock) เห็นคนละอย่าง
- **ลบ `HOME_AIRPORT` prefill** กระทบ paste ticket ([new-trip-flow.tsx:169](../apps/web/components/trip/new-trip-flow.tsx#L169)) — ticket ยังต้องเติม from/to ได้
- **ลบ native date input** กระทบ [route-builder.tsx](../apps/web/components/trip/route-builder.tsx) วันบิน + dates tab · ห้ามเหลือ `type="date"` ที่ไหนเลย ไม่งั้นรูปแบบวันไม่ตรงกันในแอปเดียว
- **ตัวละคร migration** ต้องรันก่อน deploy web ไม่งั้น user เก่ารูปหาย (fallback ทำงานแต่ทุกคนกลายเป็น flower-01)
- **variant switcher** ต้องปิดสนิทใน production (env flag) และ cookie ไม่มีผลถ้า flag ปิด
- **brand spec** หลังแก้ §2.5 ให้ grep `2.5` และ `สี = ฟีเจอร์` ทั้ง repo — Feedback #1 อ้างไว้ 3 ที่
- **Trending** ต้องมีข้อมูล view รายวันสะสมก่อน UAT อย่างน้อย 7 วัน ไม่งั้นชิปว่าง → fallback ต้องทำจริง ไม่ใช่แค่วางแผน
