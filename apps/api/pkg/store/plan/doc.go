// Package plan will hold the GORM implementation of the plans + days (A4.8) store.
//
// Placeholder — implement in the task noted above. Follow pkg/store/trip as the
// reference: a private struct holding *gorm.DB, a New() returning the interface
// declared in pkg/models, and an fx.Module. Every method must be scoped by tripID.
package plan
