/**
 * Trip overview tab: trip frame, members, status flags and quick actions,
 * plus the onboarding checklist (invite friends / add wishlist / run the AI).
 *
 * TODO(W2.2): implement.
 */
export default function TripOverviewPage() {
  return (
    <main className="space-y-2">
      <h1 className="text-xl font-semibold">ภาพรวมทริป</h1>
      <p className="text-muted text-sm">TODO W2.2 — Trip Frame, สมาชิก, quick actions</p>
    </main>
  );
}
