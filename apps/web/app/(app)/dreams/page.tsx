import { DreamScreen } from '@/components/profile/dream-screen';

/** Dream Trip bucket list (M15 — W15.1 … W15.3), on its own tab. */
export const metadata = { title: 'ที่อยากไปสักวัน' };

export default function DreamsPage() {
  return <DreamScreen />;
}
