import { VerifyScreen } from '@/components/profile/verify-screen';

/** เปิดรับรายได้ — the four-step creator verification (Feedback #4 — F11, D-22). */
export const metadata = { title: 'เปิดรับรายได้' };

export default function VerifyPage() {
  return <VerifyScreen />;
}
