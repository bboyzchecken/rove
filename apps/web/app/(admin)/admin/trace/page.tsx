import { TraceScreen } from '@/components/admin/trace-screen';

/** ไล่ที่มาแต้มและรายได้ + รายการที่ต้องตรวจ (Feedback #4 — F12, D-36). */
export default function AdminTracePage() {
  return <TraceScreen />;
}
