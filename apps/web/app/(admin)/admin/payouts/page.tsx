import { PayoutsScreen } from '@/components/admin/payouts-screen';

/** จ่ายครีเอเตอร์ — cycles, reconciliation, transfers (Feedback #4 — F11, D-21, D-40). */
export default function AdminPayoutsPage() {
  return <PayoutsScreen />;
}
