import { KycScreen } from '@/components/admin/kyc-screen';

/** ยืนยันตัวตน — the manual review queue (Feedback #4 — D-23, D-39). */
export default function AdminKycPage() {
  return <KycScreen />;
}
