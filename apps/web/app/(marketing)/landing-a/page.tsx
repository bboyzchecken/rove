import { LandingA } from '@/components/marketing/landing-a';

/** Landing A by address, for sending the tester a link (Feedback #2 — D-17). */
export const metadata = { title: 'หน้าแรก แบบ A' };

export default function LandingAPage() {
  return <LandingA />;
}
