import { LandingB } from '@/components/marketing/landing-b';

/** Landing B by address, for sending the tester a link (Feedback #2 — D-17). */
export const metadata = { title: 'หน้าแรก แบบ B' };

export default function LandingBPage() {
  return <LandingB />;
}
